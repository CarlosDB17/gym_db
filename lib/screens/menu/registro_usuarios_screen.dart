// registro_usuarios.dart
import 'package:flutter/material.dart';

class RegistroUsuariosScreen extends StatelessWidget {
  const RegistroUsuariosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Registro de Usuarios')),
      body: const Center(child: Text('Pantalla de Registro de Usuarios')),
    );
  }
}
